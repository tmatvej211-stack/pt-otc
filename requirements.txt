# Playerok bot — pinned production dependencies.
# Установка: venv/bin/pip install -r requirements.txt

# Telegram framework (основной — telebot + pyrofork для премиум-эмодзи).
pyTelegramBotAPI==4.30.0
pyrofork==2.3.69
pymediainfo-pyrofork==6.0.2
pyaes==1.6.1

# HTTP / proxy / env.
requests==2.32.5
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
PySocks==1.7.1
python-dotenv==1.2.1

# Media (для отправки видео в главном меню).
pillow==12.1.1
